  <div class="row">
    <aside class="col-md-4">
			<% if @feed_items.any? %>
				<ol class="microposts">
					<li> <%= render @feed_items %> <br/></li>
				</ol>
				<%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
			<% end %>
    </aside>
</div>
